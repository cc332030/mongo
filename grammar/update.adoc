
= update

[source,js]
----
// update each row
db.collection.find({
    name: {
        $regex: "毛"
    }
}).forEach((it) => {

    const array = it.array;

    db.collection.updateOne(
        {
            _id: it._id
        },
        {
            $set: {
                age: it.age + 1,
                array: array
            }
        }
    );
});

----
